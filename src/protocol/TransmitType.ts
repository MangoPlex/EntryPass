/**
 * Message transmit types
 */
export namespace TransmitType {

    /** Transmit user's information (mostly for generating passes) */
    export const USER_INFO = 0x0;

    /** Transmit user's pass information (for checking) */
    export const PASS_INFO = 0x1;

}